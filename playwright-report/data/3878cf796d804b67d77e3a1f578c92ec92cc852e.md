# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: public-pages.spec.ts >> Navigation >> navigates to Atuação page
- Location: tests/e2e/public-pages.spec.ts:125:3

# Error details

```
Test timeout of 30000ms exceeded while running "beforeEach" hook.
```

```
Error: page.waitForSelector: Test timeout of 30000ms exceeded.
Call log:
  - waiting for locator('nav') to be visible

```

# Page snapshot

```yaml
- generic [ref=e3]:
  - generic [ref=e4]: "15"
  - paragraph [ref=e5]: Carregando Plataforma Carlos Búrigo...
```

# Test source

```ts
  16  |     const response = await request.get('/api/settings');
  17  |     expect(response.ok()).toBeTruthy();
  18  |     
  19  |     const body = await response.json();
  20  |     expect(body.site_mode).toBeDefined();
  21  |   });
  22  | 
  23  |   test('projects endpoint returns array', async ({ request }) => {
  24  |     const response = await request.get('/api/projects');
  25  |     expect(response.ok()).toBeTruthy();
  26  |     
  27  |     const body = await response.json();
  28  |     expect(Array.isArray(body)).toBe(true);
  29  |   });
  30  | 
  31  |   test('news endpoint returns array', async ({ request }) => {
  32  |     const response = await request.get('/api/news');
  33  |     expect(response.ok()).toBeTruthy();
  34  |     
  35  |     const body = await response.json();
  36  |     expect(Array.isArray(body)).toBe(true);
  37  |   });
  38  | 
  39  |   test('agenda endpoint returns array', async ({ request }) => {
  40  |     const response = await request.get('/api/agenda');
  41  |     expect(response.ok()).toBeTruthy();
  42  |     
  43  |     const body = await response.json();
  44  |     expect(Array.isArray(body)).toBe(true);
  45  |   });
  46  | 
  47  |   test('municipalities endpoint returns array', async ({ request }) => {
  48  |     const response = await request.get('/api/municipalities');
  49  |     expect(response.ok()).toBeTruthy();
  50  |     
  51  |     const body = await response.json();
  52  |     expect(Array.isArray(body)).toBe(true);
  53  |   });
  54  | 
  55  |   test('videos endpoint returns array', async ({ request }) => {
  56  |     const response = await request.get('/api/videos');
  57  |     expect(response.ok()).toBeTruthy();
  58  |     
  59  |     const body = await response.json();
  60  |     expect(Array.isArray(body)).toBe(true);
  61  |   });
  62  | 
  63  |   test('media endpoint returns array', async ({ request }) => {
  64  |     const response = await request.get('/api/media');
  65  |     expect(response.ok()).toBeTruthy();
  66  |     
  67  |     const body = await response.json();
  68  |     expect(Array.isArray(body)).toBe(true);
  69  |   });
  70  | 
  71  |   test('votes endpoint returns array', async ({ request }) => {
  72  |     const response = await request.get('/api/votes');
  73  |     expect(response.ok()).toBeTruthy();
  74  |     
  75  |     const body = await response.json();
  76  |     expect(Array.isArray(body)).toBe(true);
  77  |   });
  78  | 
  79  |   test('documents endpoint returns array', async ({ request }) => {
  80  |     const response = await request.get('/api/documents');
  81  |     expect(response.ok()).toBeTruthy();
  82  |     
  83  |     const body = await response.json();
  84  |     expect(Array.isArray(body)).toBe(true);
  85  |   });
  86  | 
  87  |   test('evidence endpoint returns array', async ({ request }) => {
  88  |     const response = await request.get('/api/evidence');
  89  |     expect(response.ok()).toBeTruthy();
  90  |     
  91  |     const body = await response.json();
  92  |     expect(Array.isArray(body)).toBe(true);
  93  |   });
  94  | 
  95  |   test('pages endpoint returns array', async ({ request }) => {
  96  |     const response = await request.get('/api/pages');
  97  |     expect(response.ok()).toBeTruthy();
  98  |     
  99  |     const body = await response.json();
  100 |     expect(Array.isArray(body)).toBe(true);
  101 |   });
  102 | });
  103 | 
  104 | test.describe('Home page loads', () => {
  105 |   test('home page title is correct', async ({ page }) => {
  106 |     await page.goto('/');
  107 |     await page.waitForLoadState('networkidle');
  108 |     await expect(page).toHaveTitle(/Carlos Búrigo/);
  109 |   });
  110 | });
  111 | 
  112 | test.describe('Navigation', () => {
  113 |   test.beforeEach(async ({ page }) => {
  114 |     await page.goto('/');
  115 |     await page.waitForLoadState('networkidle');
> 116 |     await page.waitForSelector('nav', { timeout: 60000 });
      |                ^ Error: page.waitForSelector: Test timeout of 30000ms exceeded.
  117 |   });
  118 | 
  119 |   test('navigates to Sobre page', async ({ page }) => {
  120 |     await page.click('nav a:has-text("Sobre")');
  121 |     await page.waitForLoadState('networkidle');
  122 |     await expect(page).toHaveURL(/.*sobre/);
  123 |   });
  124 | 
  125 |   test('navigates to Atuação page', async ({ page }) => {
  126 |     await page.click('nav a:has-text("Atuação")');
  127 |     await page.waitForLoadState('networkidle');
  128 |     await expect(page).toHaveURL(/.*atuacao/);
  129 |   });
  130 | 
  131 |   test('navigates to Notícias page', async ({ page }) => {
  132 |     await page.click('nav a:has-text("Notícias")');
  133 |     await page.waitForLoadState('networkidle');
  134 |     await expect(page).toHaveURL(/.*noticias/);
  135 |   });
  136 | });
  137 | 
  138 | test.describe('Accessibility baseline', () => {
  139 |   test('home page has no critical accessibility violations', async ({ page }) => {
  140 |     await page.goto('/');
  141 |     await page.waitForLoadState('networkidle');
  142 |     await page.waitForSelector('nav', { timeout: 60000 });
  143 |     
  144 |     const accessibilityScanResults = await new AxeBuilder({ page })
  145 |       .withTags(['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa'])
  146 |       .analyze();
  147 |     
  148 |     const criticalViolations = accessibilityScanResults.violations.filter(
  149 |       (v) => v.impact === 'critical' || v.impact === 'serious'
  150 |     );
  151 |     
  152 |     expect(criticalViolations).toHaveLength(0);
  153 |   });
  154 | });
```